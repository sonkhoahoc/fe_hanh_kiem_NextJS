export enum statusCode {
  OK = 200,
  Error = 500,
}

export enum iconNotification {
  success = 'success',
  info = 'info',
  warning = 'warning',
  error = 'error',
}

export enum userType {
  student = 'student',
  admin = 'admin',
  teacher = 'teacher',
  ministry = 'ministry',
}